apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: artege
  namespace: kube-system
spec:
  entryPoints:
    - http
  routes:
    - match: Host(`artege.net`)
      kind: Rule
      services:
        - name: artege
          namespace: artege
          port: 80